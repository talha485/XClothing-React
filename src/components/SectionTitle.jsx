import React from "react";

function SectionTitle({ title }) {
  return (
    <h1 className="text-[#1F6F5F] text-[40px] mt-[30px] text-center">
      {title}
    </h1>
  );
}

export default SectionTitle;
